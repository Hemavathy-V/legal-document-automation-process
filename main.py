# DEPRECATED: contract_ai folder has been removed from the project
# This script is no longer functional. Use the FastAPI application instead:
#   cd frontend && npm run dev
#   uvicorn backend.app.main:app --reload --port 8000

# from backend.contract_ai.rag.rag_service import RAGService
# 
# if __name__ == "__main__":
#     rag = RAGService()
#     user_input = input("Enter contract requirement: ")
#     output = rag.run(user_input)
#     print(output)
